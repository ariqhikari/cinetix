import React from "react";

const NotFound = () => {
  return (
    <span className="block text-center text-sm font-medium text-accent-gray">
      Data tidak ada. <i className="text-en">Data not found.</i>
    </span>
  );
};

export default NotFound;
